import React from "react";

type Props = {};

const TicketBookingHeader = (props: Props) => {
  return (
    <div className="sticky top-0 left-0 right-0 h-16 bg-black flex items-center justify-center">
      Ticket Booking
    </div>
  );
};

export default TicketBookingHeader;
